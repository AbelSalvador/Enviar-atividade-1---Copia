#include <stdio.h>
#include <math.h>

int main() {
float aresta, volume;

printf ("Insira o valor de uma das arestas de um cubo");
scanf ("%f", &aresta);
 
volume = pow( aresta, 3);

printf ("O volume de um cubo de aresta %f, é %.3f", aresta, volume);

return 0;

}