#include <stdio.h>

int main() {
int b, h, A;

printf ("Insira a base do retângulo");
scanf ("%d", &b);

printf ("Insira a altura do retângulo");
scanf ("%d", &h);

A = b * h;

printf ("A area do retângulo é igual a %d\n", A);

return 0;

}

