#include <stdio.h>
#include <math.h>

int main() {
float raio, volume;
float pi;

printf ("Insira o valor do raio de uma esfera");
scanf ("%f", &raio);
 
pi = 3.14;

volume = (4.0/3.0) * pi * pow(raio, 3 );

printf ("O volume da esfera é igual a %.3f", volume);

return 0;

}



